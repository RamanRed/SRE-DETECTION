---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: incident-service
  namespace: sre-copilot
  labels:
    app: incident-service
    component: backend
    tier: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: incident-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: incident-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port:   "8081"
        prometheus.io/path:   "/actuator/prometheus"
    spec:
      containers:
        - name: incident-service
          image: docker.io/ramanred/sre-copilot-incident-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8081
              name: http
              protocol: TCP
          env:
            - name: JAVA_TOOL_OPTIONS
              value: "-XX:TieredStopAtLevel=1 -Xmx160m -Xms48m -XX:+UseSerialGC"
            - name: SPRING_PROFILES_ACTIVE
              valueFrom:
                configMapKeyRef:
                  name: sre-copilot-config
                  key: SPRING_PROFILES_ACTIVE
            - name: DB_HOST
              value: "sre-postgres-svc"
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: sre-copilot-config
                  key: DB_PORT
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: sre-copilot-config
                  key: DB_NAME
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: sre-db-secret
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: sre-db-secret
                  key: DB_PASSWORD
            - name: AI_COPILOT_HOST
              valueFrom:
                configMapKeyRef:
                  name: sre-copilot-config
                  key: AI_COPILOT_HOST
            - name: AI_COPILOT_PORT
              valueFrom:
                configMapKeyRef:
                  name: sre-copilot-config
                  key: AI_COPILOT_PORT
          resources:
            requests:
              memory: "200Mi"
              cpu:    "100m"
            limits:
              memory: "320Mi"
              cpu:    "400m"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8081
            initialDelaySeconds: 40
            periodSeconds: 10
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 20
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: incident-service
  namespace: sre-copilot
  labels:
    app: incident-service
spec:
  selector:
    app: incident-service
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      protocol: TCP
  type: ClusterIP
